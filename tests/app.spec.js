describe('Cycle Through Stock', () => {
  test('Return stock when there is a 2 bil market cap, 20 % change, 500k trading vol', () => {
    expect(1).toEqual(2);
  });
});

describe('checkAlertHistory', () => {
  test('todo', () => {
    expect(1).toEqual(2);
  });
});


describe('updateAlertHistory', () => {
  test('todo', () => {
    expect(1).toEqual(2);
  });
});
