// https://jasmine.github.io/2.1/introduction
// arrange

// act

// assert


// webapi returns json
// json can serialize
// post message has percentage and stock name


describe('The Logger', () => {
  it('can write to a file on the local machine', () => {
    expect(false).toBe(true);
  });
});

describe('Yahoo Api', () => {
  it('returns 3 stocks', () => {
    expect(false).toBe(true);
  });
});

describe('Slack Messenger', () => {
  it('can post a message to slack', () => {
    expect(false).toBe(true);
  });
});

describe('StockWatch Algorithm', () => {
  it('Can serialize yahoo stock json', () => {
    expect(false).toBe(true);
  });

  it('Can pull a history of whats been sent before', () => {
    expect(false).toBe(true);
  });

  it('Can write to the history file of what was just sent', () => {
    expect(false).toBe(true);
  });

  it('Wont send alert if symbol within cooldown period', () => {
    expect(false).toBe(true);
  });

  it('Will send alert when symbol off cooldown period, and update cooldown', () => {
    expect(false).toBe(true);
  });
});
